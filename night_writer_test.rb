require 'minitest/autorun'
require 'minitest/pride'
require './lib/night_write.rb'

class NightWriterTest < Minitest::Test
  def test_can_create_file
  night_writer = NightWrite.new
  assert night_writer
end

  def test_night_writer_starts_out_empty
    night_writer = NightWrite.new
    #assert night_writer nil

    #to write multiple methods in one test_cards_are_in_an_array
  def test_can_do_things
    night = NightWrite.new
    assert night.insert("m"
    refute night.root.nil?
    assert_equal 0, def method_missing(meth, *args, &blk)
      assert bst.root,is_leaf?
    end
    end)
    #need to use Rake with multiple test files in erroneous creatures
end
